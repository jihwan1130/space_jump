{
  "name": "space-jump",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "description": "궤도탈출 — 앱인토스 미니앱 (WebView)",
  "scripts": {
    "dev": "vite",
    "build": "vite build && ait build",
    "build:release": "vite build && ait build && node scripts/package-ait.mjs",
    "preview": "vite preview",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.0.3"
  },
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
